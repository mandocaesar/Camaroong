<div class="container">
    <div class="container">

        <form ng-submit="signIn()" class="form-signin">
            <div class="row">
                <div class="alert alert-success" ng-show="message == 'ended'"><h4>Due to inactivity, you were logged out automatically.</h4></div>
            </div>

            <h2 class="form-signin-heading">Please sign in</h2>
            <label for="inputEmail" class="sr-only">Email address</label>
            <input type="text" id="userName" class="form-control" ng-model="username" placeholder="Username" required autofocus>
            <label for="inputPassword" class="sr-only">Password</label>
            <input type="password" id="inputPassword" class="form-control" ng-model="password" placeholder="Password" required>
            <div class="checkbox">
                <label>
                    <input type="checkbox" value="remember-me"> Remember me
                </label>
            </div>
            <button class="btn btn-lg btn-primary btn-block" type="submit">Sign in</button>
        </form>
        <div class="row">
            <div class="col-md-3">
                <div class="row alert alert-danger margin-top-5" ng-show="showMessage">{{message}}</div>
            </div>
        </div>
    </div>

    @*<h3>Sign In</h3>

    <p>
        Before signing in, create a user at the registration page.  This template application does not have a default username and password.  After creating an account at the registration page, return to this page and sign in.
    </p>

    <form ng-submit="signIn()" class="form-signin">

        <div class="col-md-3">
            <div class="row">
                <div class="alert alert-success" ng-show="message == 'ended'"><h4>Due to inactivity, you were logged out automatically.</h4></div>
            </div>
            <div class="row"><input class="form-control margin-top-5" type="text" ng-model="username" placeholder="Username" /></div>
            <div class="row"><input class="form-control margin-top-5" type="password" ng-model="password" placeholder="Password" /></div>
            <div class="row"><input class="btn btn-primary margin-top-5" type="submit" value="Sign In" /></div>
        </div>

    </form>*@

  
</div>
